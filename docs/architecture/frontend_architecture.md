# Frontend Architecture

## Framework

Next.js App Router + React + TypeScript。

## Styling

Tailwind CSS + CSS variables。动画使用 Framer Motion，图标使用 Lucide React。

## Structure

页面放在 `src/app`，可复用组件放在 `src/components`，内容数据放在 `src/data`，类型定义放在 `src/types`。
